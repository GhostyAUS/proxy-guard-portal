import { WhitelistGroup, ProxySettings, NginxStatus } from "@/types/proxy";

// Removed mock whitelist groups as they should come from the API in production

// Removed mock proxy settings as they should come from the API in production

// Removed mock nginx status as it should come from the API in production
